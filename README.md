# AURA

Agente Unificado de Rotina Acadêmica

## Como rodar

1. instale as dependências com `npm install`
2. faça o build com `npm run build`
3. rode com `npm run start`
4. acesse http://localhost:3000

